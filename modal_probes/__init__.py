"""One-off Modal probes and smoke checks.

Throwaway verification harnesses that exercise infrastructure or engine behavior
(e.g. "does the pinned SGLang build emit routed experts for an MLA MoE?"). These
are intentionally kept out of ``cookbook/``, which holds pedagogical end-to-end
examples.
"""
