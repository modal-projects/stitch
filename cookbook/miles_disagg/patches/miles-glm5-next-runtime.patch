diff --git a/miles/backends/megatron_utils/initialize.py b/miles/backends/megatron_utils/initialize.py
--- a/miles/backends/megatron_utils/initialize.py
+++ b/miles/backends/megatron_utils/initialize.py
@@ -57,7 +57,7 @@
         order="tp-cp-ep-dp-pp" if not args.use_tp_pp_dp_mapping else "tp-cp-ep-pp-dp",
         get_embedding_ranks=get_embedding_ranks,
         get_position_embedding_ranks=get_position_embedding_ranks,
-        create_gloo_process_groups=args.enable_gloo_process_groups,
+        create_gloo_process_groups=args.use_gloo_process_groups,
     )
 
 
diff --git a/miles/backends/megatron_utils/model.py b/miles/backends/megatron_utils/model.py
--- a/miles/backends/megatron_utils/model.py
+++ b/miles/backends/megatron_utils/model.py
@@ -189,7 +189,7 @@
         optimizer = get_megatron_muon_optimizer(
             config=config,
             model_chunks=model,
-            use_gloo_process_groups=args.enable_gloo_process_groups,
+            use_gloo_process_groups=args.use_gloo_process_groups,
             layer_wise_distributed_optimizer="dist" in config.optimizer.lower(),
         )
     elif is_multi_lora_enabled(args):
@@ -200,7 +200,7 @@
         optimizer = get_megatron_optimizer(
             config=config,
             model_chunks=model,
-            use_gloo_process_groups=args.enable_gloo_process_groups,
+            use_gloo_process_groups=args.use_gloo_process_groups,
         )
 
     if args.stream_optimizer_state_to_disk:
diff --git a/miles_plugins/models/glm5/glm5.py b/miles_plugins/models/glm5/glm5.py
--- a/miles_plugins/models/glm5/glm5.py
+++ b/miles_plugins/models/glm5/glm5.py
@@ -93,6 +93,7 @@
         cp_comm_type: str | None = None,
         model_comm_pgs=None,
         pg_collection=None,
+        name: str | None = None,
     ) -> None:
 
         super().__init__(
@@ -103,6 +104,7 @@
             attn_mask_type=attn_mask_type,
             cp_comm_type=cp_comm_type,
             pg_collection=pg_collection,
+            name=name,
         )
         self.query_projection_size = self.config.v_head_dim * self.config.num_attention_heads
 
@@ -339,6 +341,7 @@
         cp_comm_type: str | None = None,
         model_comm_pgs=None,
         pg_collection=None,
+        name: str | None = None,
     ):
         super().__init__(
             config=config,
@@ -351,6 +354,7 @@
             cp_comm_type=cp_comm_type,
             model_comm_pgs=model_comm_pgs,
             pg_collection=pg_collection,
+            name=name,
         )
         q_down_proj_kwargs = {}
         if submodules.linear_q_down_proj in [TELinear]:
diff --git a/miles_plugins/models/hf_attention.py b/miles_plugins/models/hf_attention.py
--- a/miles_plugins/models/hf_attention.py
+++ b/miles_plugins/models/hf_attention.py
@@ -161,6 +161,7 @@
         layer_number: int,
         cp_comm_type: str = "p2p",
         pg_collection=None,
+        name: str | None = None,
     ):
         super().__init__(config=config)
         self.args = args
diff --git a/miles/backends/megatron_utils/megatron_to_hf/glm5_next.py b/miles/backends/megatron_utils/megatron_to_hf/glm5_next.py
--- a/miles/backends/megatron_utils/megatron_to_hf/glm5_next.py
+++ b/miles/backends/megatron_utils/megatron_to_hf/glm5_next.py
@@ -13,7 +13,7 @@
 
 import torch
 
-from .deepseekv3 import convert_deepseekv3_to_hf
+from .deepseekv3 import _to_checkpoint_dtype, convert_deepseekv3_to_hf
 
 _KDA_SUFFIX_MAPPING = {
     f"self_attention.kda.{weight_name}": f"self_attn.{weight_name}"
@@ -74,7 +74,7 @@
     return [(f"model.layers.{layer_idx}.{_HC_SITE_TO_SCALE[site]}", scale)]
 
 
-def convert_glm5_next_to_hf(args, name, param):
+def _convert_glm5_next_to_hf(args, name, param):
     match = _LAYER_PATTERN.match(name)
     if match:
         layer_idx, rest = match.groups()
@@ -97,3 +97,14 @@
             return _convert_hc_scale(layer_idx, site, alpha_name, param)
 
     return convert_deepseekv3_to_hf(args, name, param)
+
+
+def convert_glm5_next_to_hf(args, name, param):
+    converted = []
+    for hf_name, tensor in _convert_glm5_next_to_hf(args, name, param):
+        if hf_name.startswith("model."):
+            hf_name = "model.language_model." + hf_name[len("model.") :]
+        if hf_name.endswith((".hc_attn_fn", ".hc_ffn_fn", ".indexer.index_kpool_compress_ape")):
+            tensor = _to_checkpoint_dtype(args, tensor)
+        converted.append((hf_name, tensor))
+    return converted
diff --git a/miles/backends/megatron_utils/update_weight/update_weight_from_distributed/delta.py b/miles/backends/megatron_utils/update_weight/update_weight_from_distributed/delta.py
--- a/miles/backends/megatron_utils/update_weight/update_weight_from_distributed/delta.py
+++ b/miles/backends/megatron_utils/update_weight/update_weight_from_distributed/delta.py
@@ -149,7 +149,9 @@
         resumed_external = self._publish_only and self.weight_version > 0
         pulls = []
         if dist.get_rank() == 0:
-            if not resumed_external:
+            # External fleets own the run namespace. Replacing its directory
+            # leaves other hosts' shared-volume mounts writing into the old inode.
+            if not self._publish_only:
                 shutil.rmtree(self.delta_dir, ignore_errors=True)
             os.makedirs(self.delta_dir, exist_ok=True)
             if self._post_write_hook is not None:
